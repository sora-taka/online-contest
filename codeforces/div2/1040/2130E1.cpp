#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using pii = pair<int, int>;

void solve()
{
    int n;
    cin >> n;
    string s(n, ' ');
    for (int i = 0; i < n; i += 2)
    {
        int x, y;

        cout << "? 4 " << x << " " << y << " " << x << " " << y << '\n'; 

        int r;
        cin >> r;
        if (x == 1) s[i] = '(' 

            
    }
}

int main()
{
    ios::sync_with_stdio(false);  
    int t = 1;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}