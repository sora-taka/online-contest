#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using pii = pair<int, int>;
using tiii = tuple<int, int, int>;

void solve()
{
    pii x_t, y_t, x_a, y_a;
    int n, m, l;
    queue<tiii> t, a;

    cin >> x_t.first >> y_t.first >> x_a.first >> y_a.first;
    cin >> n >> m >> l;
    while(m --)
    {
        char c; int a; cin >> c >> a;
    }
}

int main()
{
    ios::sync_with_stdio(false);  
    cin.tie(nullptr), cout.tie(nullptr);  
    int t = 1;
    // cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}